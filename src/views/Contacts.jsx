import ContactForm from '../componentss/ContactForm';
import Layout from './Layout';

const Contacts = () => (
  <>
    <Layout />
    <section>
      <ContactForm />
    </section>
  </>
);

export default Contacts;
